# Задача 1. Вычислить число пи. с заданной точностью d.
# пример: при d = 0.001, выводит пи  = 3.141 \\ при d= 0.0001, выводит пи = 3.1414

# from math import pi
# d = int (input("Введите число - кол-во знаков после запятой у числа Pi = "))
# print('Число Pi  с заданной точностью = ', round(pi, d))

# или через функцию

# from math import pi
# def format_pi (num: float, accuracy_number: float) -> float:
#     """"форматирует число с плавающей запятой"""
#     accuracy_number = str(accuracy_number)
#     accuracy_number=len(accuracy_number[accuracy_number.find('.')+1::])
#     return float(f'{pi: 0.{accuracy_number}f}')
# 
# d = input('Введите точность для числа Pi в формате 0.001 = ')
# print(format_pi(pi, d))



# Задача 2. Задайте натуральное число N. Напишите программу, которая составить список простых множителей числа N.
# Например 6 -> 2,3 \\ 144 -> 2,3

# number = int(input('Введите целое число для поиска множителей = '))
# for i in range(2, number-1):
#     while(number%i==0):
#         print(i)
#         number = number/i
# 
# Задача 3. Задайте последовательность чисел. напишите прогу, которая
# выведет списрк неповторяющихся элементов исходной последовательности

# list = input('Введите числа через пробел = ').split()
# print(list)
# new_list = []
# for i in list:
#     if i not in new_list:
#         new_list.append(i)
# print(new_list)

# Задача 4. Задана натуральная степень k. Сформировать случайным образом список коэффициентов
# (значения от 0 до 100) многочлена и записать в файл многочлен степени k.(записать в строку)
# Пример:
# k=2 => 2*x^2 + 4*x + 5 или x^2 + 5 или 10*x**2
# k=3 => 2*x^3 + 4*x^2 + 4*x + 5


# 5'. Даны два файла, в каждом из которых находится запись многочлена.
# Задача - сформировать файл, содержащий сумму многочленов.
# В file1.txt :
# 2*x**2 + 4*x + 5
# В file2.txt:
# 4*x**2 + 1*x + 4
# Результирующий файл:
# 6*x**2 + 5*x + 9






